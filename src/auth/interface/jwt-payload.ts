
/* This code snippet is defining an interface named `JwtPayload` in TypeScript. The interface has a
single property `email` of type `string`. The `export` keyword makes the interface available for use
in other files/modules. This interface is likely used to define the structure of a JSON Web Token
(JWT) payload, which typically contains information about the authenticated user. */
export interface JwtPayload {
    email: string
}